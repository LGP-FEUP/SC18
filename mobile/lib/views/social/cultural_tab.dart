import 'package:flutter/material.dart';

class CulturalTab extends StatelessWidget {
  const CulturalTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Cultural");
  }
}
